/**
 * Course Preview Page
 * Shows course information in preview mode
 */
import CourseDetailPage from "../page";

export default function CoursePreviewPage() {
  return <CourseDetailPage previewMode={true} />;
}
